#include<stdio.h>

int main()
{
  int a = sizeof(int);
  int b = sizeof(char);
  int c = sizeof(float);
  int d = sizeof(double);
  printf("size of int var is %d,size of char var is %d,size of float var is %d , size of double var is %d",a,b,c,d);
  return 0;
}
